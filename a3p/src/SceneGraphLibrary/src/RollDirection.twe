enum RollDirection {
    LEFT( axis: Direction.BACKWARD ),
    RIGHT( axis: Direction.FORWARD );
    
    Direction axis;

    RollDirection( Direction axis ) {
        this.axis <- axis;
    }

    Direction getAxis() {
        return this.axis;
    }
}

