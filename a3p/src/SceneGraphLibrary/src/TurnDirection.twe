enum TurnDirection {
    LEFT( axis: Direction.UP ),
    RIGHT( axis: Direction.DOWN ),
    FORWARD( axis: Direction.LEFT ),
    BACKWARD( axis: Direction.RIGHT );

    Direction axis;

    TurnDirection( Direction axis ) {
        this.axis <- axis;
    }

    Direction getAxis() {
        return this.axis;
    }
}