enum PathStyle {
    BEE_LINE( isSmooth: false ),
    SMOOTH( isSmooth: true );

    @CompletelyHidden Boolean isSmooth;

    PathStyle( Boolean isSmooth ) {
        this.isSmooth <- isSmooth;
    }

    Boolean isSmooth() {
        return this.isSmooth;
    }
}